let SessionLoad = 1
let s:so_save = &g:so | let s:siso_save = &g:siso | setg so=0 siso=0 | setl so=-1 siso=-1
let v:this_session=expand("<sfile>:p")
silent only
silent tabonly
cd ~/.dotfiles/nvim
if expand('%') == '' && !&modified && line('$') <= 1 && getline(1) == ''
  let s:wipebuf = bufnr('%')
endif
let s:shortmess_save = &shortmess
if &shortmess =~ 'A'
  set shortmess=aoOA
else
  set shortmess=aoO
endif
badd +84 lua/user/manager.lua
badd +3 lua/user/sessions.lua
badd +17 lua/user/init.lua
badd +109 lua/user/functions.lua
badd +160 lua/user/lspconfig.lua
badd +19 lua/user/snippets.lua
badd +1 init.lua
badd +87 ~/.dotfiles/bash/.bashrc
badd +175 ~/.dotfiles/zsh/.zshrc
badd +119 ~/.dotfiles/zsh/functions.sh
badd +0 lua/user/remap.lua
argglobal
%argdel
$argadd ~/.dotfiles/nvim/
edit ~/.dotfiles/bash/.bashrc
let s:save_splitbelow = &splitbelow
let s:save_splitright = &splitright
set splitbelow splitright
wincmd _ | wincmd |
vsplit
1wincmd h
wincmd w
let &splitbelow = s:save_splitbelow
let &splitright = s:save_splitright
wincmd t
let s:save_winminheight = &winminheight
let s:save_winminwidth = &winminwidth
set winminheight=0
set winheight=1
set winminwidth=0
set winwidth=1
exe 'vert 1resize ' . ((&columns * 90 + 46) / 92)
exe 'vert 2resize ' . ((&columns * 1 + 46) / 92)
argglobal
balt init.lua
setlocal fdm=manual
setlocal fde=0
setlocal fmr={{{,}}}
setlocal fdi=#
setlocal fdl=0
setlocal fml=1
setlocal fdn=20
setlocal fen
silent! normal! zE
let &fdl = &fdl
let s:l = 176 - ((33 * winheight(0) + 21) / 42)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 176
normal! 0
lcd ~/.dotfiles/bash
wincmd w
argglobal
if bufexists(fnamemodify("~/.dotfiles/nvim/lua/user/remap.lua", ":p")) | buffer ~/.dotfiles/nvim/lua/user/remap.lua | else | edit ~/.dotfiles/nvim/lua/user/remap.lua | endif
if &buftype ==# 'terminal'
  silent file ~/.dotfiles/nvim/lua/user/remap.lua
endif
balt ~/.dotfiles/nvim/init.lua
setlocal fdm=manual
setlocal fde=0
setlocal fmr={{{,}}}
setlocal fdi=#
setlocal fdl=0
setlocal fml=1
setlocal fdn=20
setlocal fen
silent! normal! zE
let &fdl = &fdl
let s:l = 17 - ((16 * winheight(0) + 21) / 42)
if s:l < 1 | let s:l = 1 | endif
keepjumps exe s:l
normal! zt
keepjumps 17
normal! 0
lcd ~/.dotfiles/nvim
wincmd w
2wincmd w
exe 'vert 1resize ' . ((&columns * 90 + 46) / 92)
exe 'vert 2resize ' . ((&columns * 1 + 46) / 92)
tabnext 1
if exists('s:wipebuf') && len(win_findbuf(s:wipebuf)) == 0 && getbufvar(s:wipebuf, '&buftype') isnot# 'terminal'
  silent exe 'bwipe ' . s:wipebuf
endif
unlet! s:wipebuf
set winheight=1 winwidth=20
let &shortmess = s:shortmess_save
let &winminheight = s:save_winminheight
let &winminwidth = s:save_winminwidth
let s:sx = expand("<sfile>:p:r")."x.vim"
if filereadable(s:sx)
  exe "source " . fnameescape(s:sx)
endif
let &g:so = s:so_save | let &g:siso = s:siso_save
set hlsearch
doautoall SessionLoadPost
unlet SessionLoad
" vim: set ft=vim :
